const screens=[...document.querySelectorAll('.screen')];function show(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));window.scrollTo(0,0);document.body.classList.remove('menu-open');if(id==='lab')tokenize()}function fake(){toast('Mockup: ação concluída ✦')}function toast(t){let x=document.getElementById('toast');if(!x){x=document.createElement('div');x.id='toast';document.body.appendChild(x)}x.textContent=t;x.className='toast';setTimeout(()=>x.className='',2200)}function newChat(){document.getElementById('messages').innerHTML='<div class="welcome"><strong>δ</strong><h2>What can I help you build?</h2><p>Ask Delta anything.</p></div>';toast('New conversation created')}function prompt(t){document.getElementById('input').value=t;send()}function key(e){if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}function send(){const i=document.getElementById('input'),t=i.value.trim();if(!t)return;const m=document.getElementById('messages');if(m.querySelector('.welcome'))m.innerHTML='';m.innerHTML+=`<div class="msg user"><span>You</span><p>${escapeHtml(t)}</p></div>`;i.value='';setTimeout(()=>{m.innerHTML+=`<div class="msg ai"><b>δ</b><p>That’s a great direction. I’m Delta — this demo interface is ready for the real model connection. For now, I can simulate the conversation flow and showcase the product experience.</p></div>`;m.scrollTop=m.scrollHeight},500)}function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function setText(t){document.getElementById('text').value=t;tokenize()}function tokenize(){const el=document.getElementById('text'),box=document.getElementById('tokens');if(!el||!box)return;const text=el.value;const parts=text.match(/\s+|[A-Za-zÀ-ÿ]+|\d+|[^\w\s]/gu)||[];box.innerHTML=parts.map((p,i)=>p.trim()?`<span class="token">${escapeHtml(p)}</span>`:`<span style="white-space:pre">${escapeHtml(p)}</span>`).join('');const n=parts.filter(x=>x.trim()).length;document.getElementById('chars').textContent=`${text.length} characters`;document.getElementById('count').textContent=`${n} tokens`;document.getElementById('st').textContent=n;document.getElementById('sc').textContent=text.length;document.getElementById('sr').textContent=text.length?(n/text.length).toFixed(2):'0.00'}tokenize();